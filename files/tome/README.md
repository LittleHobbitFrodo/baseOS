# files/util
All the `util` subcommands (scripts) are stored and developed here. Those subcommands are in form of rust-script - simply interpretted rust.

## Notice about rust-script
`rust-script`s cache can get quite big after some time, but it can be cleared by running `rust-script --clear-chache` or via `util`: `./util cache clear`